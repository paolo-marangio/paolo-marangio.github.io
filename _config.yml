title: Paolo Marangio — Events & Community
description: A record of events, gatherings, and communities I've helped build.
theme: minima

minima:
  skin: dark

markdown: kramdown
permalink: /:title/

# This is now a USER site (its own GitHub account), so it lives at the
# domain root: https://paolo-marangio.github.io/
# No baseurl needed — leave it blank.
baseurl: ""
url: "https://paolo-marangio.github.io"

plugins:
  - jekyll-sitemap

# Empty list = don't auto-populate the top nav bar from every page.
# Your own "Events" links on the homepage act as navigation instead.
header_pages: []
